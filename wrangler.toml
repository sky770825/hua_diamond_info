# Cloudflare Pages - hua_diamond_info
# 部署：npm run deploy 或 npm run deploy:pages
# 首次需先執行：npx wrangler login

name = "hua-diamond-info"
compatibility_date = "2024-01-01"

# Pages 建置輸出目錄（由 npm run build 產生）
# 實際部署指令：wrangler pages deploy dist --project-name=hua-diamond-info
